{
  "name": "BudgetKit",
  "user": "node",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @budgetkit/core build && pnpm --filter @budgetkit/db build",
  "terminals": [
    {
      "name": "dev",
      "command": "pnpm dev"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 5173
    },
    {
      "name": "api",
      "port": 3000
    }
  ]
}
